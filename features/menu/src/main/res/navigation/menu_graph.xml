<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menu_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.dvm.menu.main.MainFragment"
        android:label="MainFragment">
        <action
            android:id="@+id/toMenu"
            app:destination="@id/menuFragment" />
    </fragment>

    <fragment
        android:id="@+id/menuFragment"
        android:name="com.dvm.menu.menu.presentation.MenuFragment"
        android:label="MenuFragment">
        <action
            android:id="@+id/toCategory"
            app:destination="@id/categoryFragment" />
        <action
            android:id="@+id/toSearch"
            app:destination="@id/searchFragment" />
    </fragment>

    <fragment
        android:id="@+id/categoryFragment"
        android:name="com.dvm.menu.category.presentation.CategoryFragment"
        android:label="CategoryFragment" >
        <argument
            android:name="id"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.dvm.menu.search.SearchFragment"
        android:label="SearchFragment" />
</navigation>